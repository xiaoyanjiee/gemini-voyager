# V2 Testing Gate

Run format checking, zero-warning lint, type checking, coverage, Chrome/Edge/Firefox builds, docs build, production dependency audit, and `git diff --check` before release.

Manual acceptance requires signed-in M365, Google Drive, and OneDrive sessions. Verify long virtualized conversations, VoyagerDock, Popup, organization, prompt insertion without sending, selected export, copying, conflict resolution, and primary-cloud switching.
